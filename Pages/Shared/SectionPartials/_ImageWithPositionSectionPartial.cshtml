@model ImageWithPositionSection

@{
    var imagePosition = Model.ImagePosition;
}

<section class="section-full @(!string.IsNullOrWhiteSpace(Model.BackgroundColor) ? $"{Model.BackgroundColor} pt-3 pb-3" : "")">
    <div class="container">
        @if (!string.IsNullOrWhiteSpace(Model.Heading))
        {
            <h2 class="text-green sci-fi-text">@Model.Heading</h2>
        }
        @if (!string.IsNullOrWhiteSpace(Model.SubHeading))
        {
            <h3 class="text-green sci-fi-text">@Model.SubHeading</h3>
        }
        @switch (imagePosition)
        {
            case ImagePosition.Top:
            case ImagePosition.Bottom:
                <partial name="/Pages/Shared/ComponentPartials/_ImagePositionYPartial.cshtml" />
                break;

            case ImagePosition.Left:
            case ImagePosition.Right:
                <partial name="/Pages/Shared/ComponentPartials/_ImagePositionXPartial.cshtml" />
                break;
        }
    </div>
</section>
@if (Model.SpaceBottm)
{
    <div class="section-space"></div>
}