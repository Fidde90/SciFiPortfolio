@model CardSection
@{
}
<section class="section-full @(Model.BackgroundColor ? "section-bg pt-3 pb-3" : "")">
    <div class="project-cards container">
        @if (!string.IsNullOrWhiteSpace(Model.Heading))
        {
            <h2 class="text-green sci-fi-text">@Model.Heading</h2>
        }
        @foreach (var card in Model.Cards)
        {
            <partial name="/Pages/Shared/ComponentPartials/_ProjectCardPartial.cshtml" model="card" />
        }
    </div>
</section>
@if (Model.SpaceBottm)
{
    <div class="section-space"></div>
}